export class FavoriteRepositoryPort {
  load() { throw new Error("FavoriteRepositoryPort.load no implementado"); }
  save(_favorites) { throw new Error("FavoriteRepositoryPort.save no implementado"); }
}
