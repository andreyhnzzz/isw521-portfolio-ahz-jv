export class ConversationRepositoryPort {
  load() { throw new Error("ConversationRepositoryPort.load no implementado"); }
  save(_messages) { throw new Error("ConversationRepositoryPort.save no implementado"); }
  clear() { throw new Error("ConversationRepositoryPort.clear no implementado"); }
}
